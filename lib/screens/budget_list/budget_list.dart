import 'package:flutter/material.dart';

class BudgetList extends StatelessWidget {
  const BudgetList({super.key});

  static const routName = 'budget-list';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
